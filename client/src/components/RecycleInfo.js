const RecycleInfo = () => {
	return (
		<div>
			<p>
				Enter your city and state, then press, then press 'Find Locations' to
				find recycling centers near you. You can also try adding a specific
				material to your search to find recycling centers that can handle that
				material.
			</p>
		</div>
	);
};

export default RecycleInfo;
